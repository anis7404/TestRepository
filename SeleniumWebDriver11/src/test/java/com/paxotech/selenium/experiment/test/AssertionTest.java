package com.paxotech.selenium.experiment.test;

//import org.testng.Assert;
//import static org.testng.Assert.assertTrue;
import static org.testng.Assert.*;

import org.hamcrest.core.IsEqual;
import org.testng.annotations.Test;

import static org.hamcrest.Matchers.*;
//import static org.hamcrest.CoreMatchers.*;
import static org.hamcrest.MatcherAssert.*;
import static com.paxotech.selenium.framework.matcher.PatternMatcher.*;

public class AssertionTest {
	
/*	@Test
	public void test(){
		
		//Assert as = new Assert();
		//as.assertEquals(actual, expected);
		
		String myEmpty = null;
		
		
		System.out.println("Test Starting");
		
		String myExpectedName = "Iftekhar";
		String myActualName = "iftekhar";
		
		String msg = "Actual <" + myActualName + ">  is not same as expected <" + myExpectedName + ">";
		
		//Assert.assertEquals(myActualName.toUpperCase(), myExpectedName.toUpperCase());
		assertTrue(myActualName.contentEquals(myExpectedName), msg);
		
	
		
		System.out.println("Test Ending");
		
	}
	
	@Test
	public void test2(){
		String myExpectedName = "Iftekhar";
		String myActualName = "Iftekhar";
		
		assertThat(myActualName, containsString(myExpectedName));
		assertThat(myActualName, is(myExpectedName));
		assertThat(myActualName, not(is(myExpectedName)));
	}
	
	@Test
	public void test3(){
		String myExpectedName = "Iftekhar";
		String myActualName = "Iftekhar Ivaan";
		
		//assertThat(myActualName, pattern("Iftekhar"));


		String text = "Plan cost $99.00";
		
		assertThat(text, pattern(".*\\$[0-9][0-9]\\.[0-9][0-9]$"));
		
		
	}*/

}
